import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Product — Torvus Security",
  description:
    "From simple setup to strong guarantees: encrypt locally, set conditions, release only when it’s right.",
};